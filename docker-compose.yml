version: '3.9'
services:
  parsival:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      REDIS_ENABLED: "false"
      PYTHONUNBUFFERED: "1"
    volumes:
      - .:/app:ro
